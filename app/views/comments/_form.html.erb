<h3>Post a Comment</h3>

<%= form_for [ @post, @comment ] do |f| %>

  <p>
    <%= f.label :body %><br/>
  <%= f.text_area :body, placeholder: "Add a comment" %><br/>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
