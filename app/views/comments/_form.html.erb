<h3>Post a Comment</h3>

<%= form_for [ @post, @comment ] do |f| %>

  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body, placeholder: "What do you think? #{current_user.username}?" %><br/>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>


<% end %>
