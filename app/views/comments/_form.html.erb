
<%= form_for [ @post, @comment ] do |f| %>

  <p>
    <%= f.label :body %></br>
    <%= f.text_area :body, placeholder: "Add a reply #{current_user.username}?" %><br/>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>



<% end %>
